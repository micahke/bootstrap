project: bootstrap
description: A small tool to analyze your projects using AI

filetypes: [".py"]
excluded_dirs: [".venv", "build", "bootstrap.egg-info", ".git", ".github"]

llm:
  model: gpt4t

index:
  type: vector
  verbose: true

query:
  temperature: 0.1
  max_tokens: 4096
